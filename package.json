{
  "name": "btc-payments",
  "version": "1.1.6",
  "description": "Bitcoin payments processor built over using bitcore and blocker.io api.",
  "main": "./lib/index",
  "dependencies": {
    "async": "^0.9.2",
    "bitcore-lib": "^0.13.7",
    "just-a-logger": "^0.1.1",
    "mongoose": "4.1.8",
    "request": "^2.40.0"
  },
  "devDependencies": {},
  "scripts": {
    "start": "node lib/index",
    "test": "mocha test/complete -u tdd --timeout 0",
    "test-complete": "mocha test/complete -u tdd --timeout 0",
    "test-payment": "mocha test/createPayment -u tdd --timeout 0",
    "test-functions": "mocha test/functions -u tdd --timeout 0",
    "test-run": "mocha test/run -u tdd --timeout 0"
  },
  "keywords": [
    "Bitcoin",
    "Payment",
    "processor",
    "Bitcore",
    "Blockr.io"
  ],
  "author": "Stephen Adhokorie",
  "license": "MIT",
  "directories": {
    "test": "test"
  }
}
